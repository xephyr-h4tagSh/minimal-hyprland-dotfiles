# minimal-hyprland-dotfiles
my first minimal hyprland dotfiles! :000

to use, download the config folder, move it to your /home/(username)/ and rename it to .config

for arch+hyprland, also get the dependencies:
waybar, rofi

you may also want to run this command as your user: sudo rm -rf ~/.config before doing any of this
